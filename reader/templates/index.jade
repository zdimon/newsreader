ion-view(title="ТОП10" id="page1")
    ion-content(padding="true" class="has-header")
        div
            ion-list
                ion-item(class="item-thumbnail-left" ng-repeat="a in top_list" ng-click="go(a.id)")
                    //a(ui-sref="reader.journals" class="item-thumbnail-left")
                    img(ng-src="{{ a.small_image_portrait }}" class="top10_img_link" )
                    h2 {{ a.title }}
                    p {{ a.author }}
                    p {{ a.journal }}
                    p
                        {{ a.short_text }}
